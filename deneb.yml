server:  
  port: ${PORT:2222}

eureka:  
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

cygnus:  
  sadr:
    name: sadr
    pingUrl: http://${cygnus.sadr.name}/sadr/ping/
